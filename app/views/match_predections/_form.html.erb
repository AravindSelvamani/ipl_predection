<%= form_with(model: match_predection, local: true) do |form| %>
  <% if match_predection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match_predection.errors.count, "error") %> prohibited this match_predection from being saved:</h2>

      <ul>
        <% match_predection.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :winners %>
    <%= form.text_field :winners %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
