<% if false %>
<h1>Editing Match Predection</h1>

<%= render 'form', match_predection: @match_predection %>

<%= link_to 'Show', @match_predection %> |
<%= link_to 'Back', match_predections_path %>

<% end %>